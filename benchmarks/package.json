{
  "name": "kittens-crew-benchmarks",
  "private": true,
  "type": "module",
  "scripts": {
    "bench": "bun bench.ts",
    "bench:agentic": "cd agentic && bun run.ts",
    "chart:agentic": "cd agentic && bun chart.ts"
  },
  "dependencies": {
    "gpt-tokenizer": "^2.9.0"
  }
}
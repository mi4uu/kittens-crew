# This project ships a skillset — use it

The commands and skills installed here are the intended workflow, not optional
extras. Before you plan or write code from memory: check whether an available
skill or command fits the task. If one does, load or run it FIRST — even if you
already think you know the answer. Only work unaided when nothing fits.

<!--
  Benchmark note: this nudge is DELIBERATELY generic + short + identical across
  every skill-based arm (kittens / cavekit / ponytail). It names no specific skill
  and tunes for no specific outcome — its only job is to counter the model's
  default "I'll just do it myself", which otherwise leaves every skillset untouched
  and unmeasurable. Baseline gets no CLAUDE.md (it has no skillset to reach for).
  Keep it neutral: do not let it juice any one arm.
-->

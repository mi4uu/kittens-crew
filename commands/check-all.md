---
description: Read-only BLOAT hunt across the whole repo (the periodic audit). Writes nothing.
---

Invoke the **check** skill in BLOAT mode, scope = **whole repo**.

😼 Entropy Kitty sweeps every file against the ladder, in reverse: OVER-ENGINEERED
abstractions, REINVENTED stdlib/native, DUPLICATED logic (DRY), and `// kitten:`
CEILING-HIT shortcuts the spec outgrew. Rank by what to delete first. Report
grouped by severity, file:line evidence, one remedy hint per item. Slow; this is
the periodic audit, not the per-commit pass. Write nothing. Main thread reads.
